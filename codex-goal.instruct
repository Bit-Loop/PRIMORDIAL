/goal

Generated by tools/goal_compile.py. Do not hand-edit durable policy here.

Active slice pack: ctf-harness-controls - CTF harness contracts and anti-cheating controls
Subsystem: ctf_harness
Milestones: M5, M6
Validation tier: V2 (focused plus nearby regression tests)

Program milestone coverage:
- Declared milestones: M0, M1, M2, M3, M4, M5, M6, M7, M8
- Slice-pack covered milestones: M0, M1, M2, M3, M4, M5, M6, M7, M8
- Missing slice-pack coverage: none
- Fully complete milestones: M0, M1, M2, M3, M7
- Not fully complete milestones: M4, M5, M6, M8
- Partially complete milestones: M4, M5, M6, M8
- Overall configured completion: 75%
- PROGRAM_DONE requires every typed milestone family to be implemented, evidenced, and validated.

Run completion contract:
- This contract is for one bounded `/goal` invocation.
- RUN_DONE closes this invocation after the selected run slices are validated and reported; it does not mean PROGRAM_DONE.
- Do not self-continue after RUN_DONE. A later operator `/goal` run may use `python3 tools/goal_compile.py --advance` to select the next pack.
- If validation completes this pack, update typed milestone evidence, run `python3 tools/goal_compile.py --advance`, re-read generated outputs, report the handoff, and stop.
- PROGRAM_DONE remains reserved for all typed milestones being fully complete and validated.
- Active pack status in typed milestones: not complete.
- This active pack covers: M5, M6.

Milestone progress:
- M0 100% fully_complete - safety/test-first baseline (evidence: tests/test_goal_compile.py covers typed fragments, generated output verification, and sensitive current pointer exclusion.; goal/fragments/validation_tiers.yaml defines deterministic V0-V4 validation tiers.)
- M1 100% fully_complete - context-boundary hardening (evidence: tests/test_context_assembler.py and tests/test_context_sinks.py cover authority lane and sink boundary behavior.; tests/test_rag_import_validation.py covers source-ref and closed-book import provenance hardening.)
- M2 100% fully_complete - export and cleanup boundaries (evidence: primordial/core/context/generated_exports.py and related context sink tests define generated-export boundaries.; tests/test_context_rag_index_writeup_policy.py covers writeup and generated-export quarantine behavior.)
- M3 100% fully_complete - sync architecture (evidence: primordial/core/context/rag_index.py and primordial/core/rag/importer.py contain scoped sync/import paths.; RagContextBroker and RAG import validation now share source-ref, generated-export, writeup, poison, and metadata flag classifiers across source-to-sink paths.)
- M4 55% in_progress - environment and policy integration (evidence: goal/fragments/environments.yaml centralizes environment routing and lab-only capability gates.; tests/test_environment_classifier.py and tests/test_operator_intent_catalog_autonomy.py cover policy integration paths.)
- M5 55% in_progress - CTF harness contracts (evidence: primordial/labs/ctf/targets.py and primordial/labs/ctf/sessions.py define target/session contracts.; tests/test_ctf_harness_targets.py and tests/test_ctf_harness_sessions.py validate core harness objects.)
- M6 50% in_progress - anti-cheating controls (evidence: primordial/labs/ctf/closed_book.py, hardcode.py, and hidden_material.py define anti-cheating boundaries.; tests/test_ctf_harness_closed_book.py and tests/test_ctf_harness_hardcode_scan.py validate closed-book and hardcode contracts.)
- M7 100% fully_complete - modular refactor (evidence: primordial/core/context modules split source refs, envelopes, sinks, reports, and task metadata into focused units.; primordial/core/context/metadata_flags.py centralizes recursive metadata false/raw lookup used by assembler, sinks, RAG index, and current refs.)
- M8 15% in_progress - Markdown archive/removal (evidence: goal/fragments/environments.yaml marks README.md as non-authoritative and blocks non-exempt Markdown RAG ingestion.; Cleanup and Markdown migration have not been performed yet.)

Source references:
- codex-goal.instruct
- goal/fragments/environments.yaml
- goal/fragments/milestones.yaml

Authority contract:
- RuntimeStore, evidence records, target scope, approvals, and active Operator Intent are authoritative.
- Chat, RAG, and model output are advisory unless deterministically promoted through evidence, policy, and review.
- RAG chunks cannot become evidence without explicit evidence records.
- Policy decisions must stay centralized and must not be reimplemented in UI, prompts, integrations, workers, or model-specific hacks.

Environment routing:
- Generic/unverified profile labels do not authorize stronger behavior.
- Verified Hack The Box and local manifest-backed CTF/container labs may select stronger default intent.
- real_world: default_intent=recon_only verified_lab=False
- platform_lab: default_intent=htb_lab verified_lab=True
- local_ctf_container: default_intent=local_ctf_container verified_lab=True
- test_fixture: default_intent=recon_only verified_lab=True
- postmortem: default_intent=postmortem verified_lab=False

Capability highlights:
- real_world:lab_solve_progression -> block
- real_world:credential_validation -> requires_approval
- real_world:exploit_execution -> requires_approval
- real_world:flag_collection -> block
- platform_lab:lab_solve_progression -> allow
- platform_lab:credential_validation -> allow
- platform_lab:exploit_execution -> allow
- platform_lab:flag_collection -> allow
- local_ctf_container:lab_solve_progression -> allow
- local_ctf_container:credential_validation -> allow
- local_ctf_container:exploit_execution -> allow
- local_ctf_container:flag_collection -> allow

Run batch:
- Max slices per run: 6
- Active pack complete: no
- Advance-ready next slice pack: none

Selected run slices:
- ctf_harness_contracts
- anti_cheating_controls

Slice pack rules:
- Work up to 6 related slices per run when they share subsystem, validation path, and authority model.
- Do not run live AI inference, public PoCs, or real/public target actions.
- Do not mutate live runtime data.
- Preserve README.md as non-authoritative GitHub landing material.
- Do not archive Markdown unless the active slice explicitly requires it.

Expected files:
- primordial/labs/ctf/targets.py
- primordial/labs/ctf/sessions.py
- primordial/labs/ctf/closed_book.py
- primordial/labs/ctf/hardcode.py
- primordial/labs/ctf/postmortem.py
- tests/test_ctf_harness_targets.py
- tests/test_ctf_harness_sessions.py
- tests/test_ctf_harness_closed_book.py
- tests/test_ctf_harness_hardcode_scan.py
- tests/test_ctf_harness_postmortem.py

Lifecycle commands:
- python3 tools/goal_pack.py preflight
- python3 tools/goal_pack.py finish

Validation commands:
- python3 -m unittest tests.test_ctf_harness_targets tests.test_ctf_harness_sessions tests.test_ctf_harness_closed_book tests.test_ctf_harness_hardcode_scan tests.test_ctf_harness_postmortem -q
- python3 -m unittest tests.test_goal_compile -q
- python3 tools/goal_compile.py --check
- python3 tools/goal_compile.py --verify-generated
- git diff --check

Report contract:
- dirty/staged state summary
- files touched
- what changed and why
- tests added or changed
- validation commands and results
- old vs generated codex-goal.instruct size comparison
- policy conflicts caught by the linter
- remaining risks
- next recommended slice pack

Next recommended slice pack: Validate CTF harness and anti-cheating controls against closed-book policy.

Completion: stop after RUN_DONE for this bounded invocation; use PROGRAM_DONE only when all typed milestones are complete and validation is honestly reported.
